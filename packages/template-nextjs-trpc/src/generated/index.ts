/**
 * This is where you keep your generated files (if any)
 *
 * Reason we have a dedicated folder for them is that
 * it makes it easier to disable eslint checks on
 * generated files if they're all within the same folder,
 * as all you need to do to ignore eslint checks is to add
 * the folder path to your .eslintignore
 */
